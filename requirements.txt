# Core pipeline dependencies (precompute.py and rank.py)
# Install torch first with CPU-only wheel:
#   pip install torch==2.3.1+cpu --index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
sentence-transformers==2.7.0
numpy==1.26.4
